
Full blog app (frontend + backend).

BACKEND:
  cd backend
  npm install
  npm start
  - Server will run at http://localhost:5000
  - db.json holds users and blogs

FRONTEND:
  cd frontend
  npm install
  npm start
  - React app will run at http://localhost:3000
  - The app uses the backend at http://localhost:5000
